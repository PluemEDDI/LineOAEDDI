{
  "_note": "Per-page overrides for the structure-driven walker. Each entry forces an attribution the walker can't infer from layout alone (e.g. a heading appears on one page but its table actually belongs to an older queued section because table content matches the older heading semantically). Should stay small. The build gate (C2) will fail if a page is in overrides but doesn't need it.",
  "bodyOverrides": {
    "20": "1.3.4",
    "23": "1.3.6",
    "26": "1.3.7"
  },
  "imageOverrides": {
    "23": "1.3.6"
  }
}
